{
  "name": "@litegraph/tests",
  "version": "0.1.0",
  "description": "",
  "main": "./src/main.ts",
  "types": "./src/main.ts",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@litegraph/core": "workspace:*",
    "@litegraph/nodes-basic": "workspace:*"
  },
  "devDependencies": {
    "@litegraph/tsconfig": "workspace:*",
    "typescript": "^5.0.3",
    "vite": "^4.2.1",
    "vitest": "^0.31.0"
  }
}
